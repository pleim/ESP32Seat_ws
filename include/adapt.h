#pragma once






/*
class Adapt
{
    public:
    Adapt(int mn, int mx)
        : min(mn), max(mx)
    { };

    ~Adapt() = default;

    // Adapt input
    // @returns value in %, min = 0%, max = 100%
    float operator () (int in)
    {
        float a = in - min;
        float b = max - min;
        return a/b * 100.0f;
    };

    int min;
    int max;
};
*/